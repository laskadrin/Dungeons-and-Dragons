import { StatsTranslationPipe } from './stats-translation.pipe';

describe('StatsTranslationPipe', () => {
  it('create an instance', () => {
    const pipe = new StatsTranslationPipe();
    expect(pipe).toBeTruthy();
  });
});
